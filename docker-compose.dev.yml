version: "3.9"

services:
  whoknows:
    build:
      context: go
      dockerfile: Dockerfile.dev
    container_name: whoknows-go
    ports:
      - "8080:8080"
    volumes:
      - ./database.db:/whoknows/database.db
    env_file:
      - go/.env
    restart: unless-stopped