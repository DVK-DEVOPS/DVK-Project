services:
  whoknows:
    image: ghcr.io/dvk-devops/dvk-project:latest
    environment:
      - KEYVAULT_NAME=${KEYVAULT_NAME}
      - SECRET_NAME=${SECRET_NAME}
      - API_URL=${API_URL}
    container_name: whoknows-prod
    ports:
      - "8080:8080"
    volumes:
      - ./go/database.db:/whoknows/go/database.db
    networks:
      - webnet
    restart: unless-stopped

  nginx:
    image: ghcr.io/dvk-devops/nginx:latest
    container_name: whoknows-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    environment:
      - LETSENCRYPT_DOMAIN=${LETSENCRYPT_DOMAIN}
    volumes:
      - /etc/letsencrypt/live/${LETSENCRYPT_DOMAIN}:/etc/letsencrypt/live/${LETSENCRYPT_DOMAIN}:ro
      - /etc/letsencrypt/archive/${LETSENCRYPT_DOMAIN}:/etc/letsencrypt/archive/${LETSENCRYPT_DOMAIN}:ro
    depends_on:
      - whoknows
    networks:
      - webnet

networks:
  webnet:
    driver: bridge